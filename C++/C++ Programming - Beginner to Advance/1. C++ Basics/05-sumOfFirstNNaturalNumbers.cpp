#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter the a number: ";
    cin >> n;
    cout << "Sum of first N natural numbers is " << n * (n + 1) / 2;
    return 0;
}